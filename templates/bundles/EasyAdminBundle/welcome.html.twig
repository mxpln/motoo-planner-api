{# @var ea \EasyCorp\Bundle\EasyAdminBundle\Context\AdminContext #}
{% extends '@EasyAdmin/page/content.html.twig' %}

{% block page_title 'Tableau de bord' %}

{% block page_content %}

    <div class="container">
        <div class="row">
            <div class="col-6 alert-info">
                <span class="display-3"> {{ countAllUser['value'] }} </span>Utilisateurs
            </div>
            <div class="col-6 alert-success">
                <span class="display-3"> {{ countAllRoadbook['value'] }} </span>Roadbooks
            </div>
        </div>

        <div class="row">
            <div class="col-12 mt-5">
                <h3 class="mb-3">Aperçu des articles</h3>
                <ul class="list-group">
                    {% for article in articles %}
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            {{ article.title }}
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>

    <pre>
    </pre>
{% endblock %}
